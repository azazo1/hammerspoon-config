return {
  ctrl_prefix_bracket = {
    timeout_seconds = 0.5,
    switch_delay_seconds = 0.02,
  },
  ime_hotkey = {
    -- Upstream maps right Shift to English.
    english = {
      modifiers = {},
      key = "rightshift",
    },
  },
}
